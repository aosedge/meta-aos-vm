# iptables addrtype match - Netavark (podman's network backend, used by the benchmark image) uses it
# for its port-mapping DNAT chain ("-m addrtype --dst-type LOCAL"); without it every container start
# fails

CONFIG_NETFILTER_XT_MATCH_ADDRTYPE=m
