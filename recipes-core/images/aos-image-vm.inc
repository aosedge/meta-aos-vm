# Configuration

BOARD_MODEL ?= "vm-dev;1.0"

BUNDLE_IMAGE_VERSION ?= "2.0.2"

ROOTFS_IMAGE_VERSION ?= "${BUNDLE_IMAGE_VERSION}"
ROOTFS_REF_VERSION ?= "${BUNDLE_IMAGE_VERSION}"
BOOT_IMAGE_VERSION ?= "${BUNDLE_IMAGE_VERSION}"
